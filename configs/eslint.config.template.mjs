// eslint.config.mjs — Generated by opencode-antigravity template
// Customize after Phase 0 stack selection.
// Docs: https://eslint.org/docs/latest/use/configure/configuration-files

import js from "@eslint/js";
// Uncomment for TypeScript:
// import tseslint from "typescript-eslint";
// Uncomment for React:
// import react from "eslint-plugin-react";
// import reactHooks from "eslint-plugin-react-hooks";

export default [
  js.configs.recommended,

  // Uncomment for TypeScript:
  // ...tseslint.configs.recommended,

  // Uncomment for React:
  // react.configs.flat.recommended,
  // react.configs.flat["jsx-runtime"],

  {
    // Global ignores
    ignores: [
      "node_modules/",
      "dist/",
      ".next/",
      "build/",
      "coverage/",
    ],
  },

  {
    // Component-First Architecture enforcement
    rules: {
      // Chặn file quá lớn (signal cần tách component)
      "max-lines": ["warn", { max: 300, skipBlankLines: true, skipComments: true }],

      // Chặn function quá dài (50-line JSX rule)
      "max-lines-per-function": ["warn", { max: 80, skipBlankLines: true, skipComments: true }],

      // Không dùng console.log trong production
      "no-console": ["warn", { allow: ["warn", "error"] }],

      // Bắt buộc const/let, không var
      "no-var": "error",
      "prefer-const": "error",

      // Không dùng any (bật khi dùng TypeScript)
      // "@typescript-eslint/no-explicit-any": "warn",

      // Unused vars
      "no-unused-vars": ["warn", { argsIgnorePattern: "^_" }],
    },
  },

  // Uncomment for React hooks rules:
  // {
  //   plugins: { "react-hooks": reactHooks },
  //   rules: reactHooks.configs.recommended.rules,
  // },
];
